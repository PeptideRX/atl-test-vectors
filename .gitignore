node_modules/
*.log
.DS_Store
.env
.env.local

# Generated fresh on every run of produce-and-verify-fresh.mjs.
# No point in checking in.
vector-fresh.json

# package-lock.json is intentionally committed. The verifier dependency
# is pinned exactly so this repo functions as a frozen reproduction
# harness, not a moving target.
